# parallel-viewer

分割ビューアー（v3.5.0 KENBANから完全移植）。2パネル独立/同期モード切替、PDF全ページ自動展開、A/B双方が揃った時のみ読み込み（v3.8.1）。全画面時は画像エリアのみ表示（OSフルスクリーン）。

**関連**: `components/parallel-viewer/ParallelViewerView.tsx`、`store/parallelStore.ts`、Rust: `kenban_*` コマンド。
